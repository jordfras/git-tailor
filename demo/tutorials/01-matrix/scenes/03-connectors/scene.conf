TITLE="Connectors, and red"
NARRATION_DELAY=0.6
TITLE_AT=0.4
TITLE_HOLD=3.0

MARKERS=(
    "4.6 6.0 74 4 1 1"      # the connector alone, on the commit it crosses
    # Two boxes rather than one around both: the column between them is not
    # red, and a box that swallows it marks a run of three.
    "14.0 5.0 73 6 1 1"     # one red square, leading back one way
    "14.0 5.0 75 6 1 1"     # the other, leading back elsewhere
)
