# Scene metadata, sourced by demo/promo/scripts/compose.sh.
TITLE="4 minutes later"
SUBTITLE=""
NARRATION_DELAY=0.4
TITLE_HOLD=2.0
GRAYSCALE=1
# Fast-forward the keystrokes; the narration plays at normal speed over them.
# Tracked to the narration: the scene lasts whichever of the two is longer, so
# winding the capture past the point where the voice takes over buys nothing,
# and leaving it short of that point strands the tail of the scene in silence.
# Retune this whenever the script changes.
SPEED=2.27
SFX=none
MUSIC_DULL=1
