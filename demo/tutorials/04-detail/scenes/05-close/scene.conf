TITLE="Reading, not doing"
NARRATION_DELAY=1.5
TITLE_AT=0.4
TITLE_HOLD=2.8

# Timed against the voice: line 2 starts at 7.54s and line 3 at 17.47s, plus
# NARRATION_DELAY. The first cue clears before the overlay is scrolled: it sits
# at the bottom of the frame, which is exactly where the end of the list
# arrives, and a caption over the lines it just scrolled to is worse than a
# short one.
CAPTIONS=(
    "9.2 1.9 - h lists the reading keys here"
    "19.0 3.0 - q first, then the keys work again"
)
