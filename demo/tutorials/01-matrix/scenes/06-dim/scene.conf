TITLE="Dim means something else"
NARRATION_DELAY=0.6
TITLE_AT=0.4
TITLE_HOLD=3.0

# The leftmost column is the selected commit's own, and stays bright, so the
# dim ones the narration names are the other three.
MARKERS=(
    "6.6 3.7 73 1 3 7"      # the columns this commit never touched
    "11.2 4.9 11 2 46 1"    # dim green in the list: folds, but not into this
    "12.9 3.2 11 5 46 1"
)
